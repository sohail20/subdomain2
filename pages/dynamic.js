import React from 'react';

function DynamicPage({ subdomain }) {
    return (
        <div>
            hello world123 polly world
            DynamicPage {subdomain}
        </div>
    );
}

export default DynamicPage;
